<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lorentzfactorcalculation">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Lorentz factor calculation"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LorentzFactorCalculation">
        <activity android:name=".MainActivity4"
            android:label="Spi factor Calculation"
            android:parentActivityName=".MainActivity3" ></activity>
        <activity android:name=".MainActivity3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Lorentz factor calculation"
            android:parentActivityName=".MainActivity3" ></activity>
        <activity
            android:name=".MainActivity2"
            android:label="Check for Lorentz factor"
            android:parentActivityName=".MainActivity" />
    </application>
    <uses-permission android:name="android.permission.VIBRATE"/>
</manifest>